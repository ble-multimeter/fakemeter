# Runtime deps. Install into a venv created with --system-site-packages so the
# distro-provided python3-dbus / python3-gi (PyGObject) are visible — those are
# painful to pip-build, and bluezero needs them.
bluezero>=0.9.0

# Dev/test only:
pytest>=8.0
